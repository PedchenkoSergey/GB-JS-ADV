const sum = (x, y) => {
    if ((x == undefined) || (y == undefined) || (typeof(x) != Number) || (typeof(y) != Number)) {
        return 0
    }
        return
    return x + y
}

const sub = (x, y) => {
    if ((x == undefined) || (y == undefined) || (typeof(x) != Number) || (typeof(y) != Number)) {
        return 0
    }
        return
    return x - y
}

const mul = (x, y) => {
    if ((x == undefined) || (y == undefined) || (typeof(x) != Number) || (typeof(y) != Number)) {
        return 0
    }
        return
    return x * y
}

const division = (x, y) => {
    if ((x == undefined) || (y == undefined) || (typeof(x) != Number) || (typeof(y) != Number)) {
        return 0
    }
        return
    return x / y
}

